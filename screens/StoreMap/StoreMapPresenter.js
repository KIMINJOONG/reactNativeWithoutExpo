import React from 'react';
import styled from 'styled-components';

const Container = styled.View`
    
`;

const Text = styled.Text`
`;
const StoreMapPresenter = ({}) => (
    <Container>
        <Text>
            맵
        </Text>
    </Container>
);

export default StoreMapPresenter;
